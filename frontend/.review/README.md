# Fixture visive locali

Avvio dalla cartella frontend: `npm exec vite -- --config .review/vite.config.js`.

Server solo 127.0.0.1:5180, API e account sintetici via alias Vite esclusivo di questa configurazione. Aprire /analisi e inviare il modulo per vedere un report; aprire /dashboard e scegliere il mezzo dimostrativo per verificare la finestra. Checkout, invio lead e richieste reali sono disabilitati. Nessuna modifica a localStorage o autenticazione reale. Questi file non sono importati nell'entrypoint di produzione.

